We recently tried implementing the plan in `PLAN.md` and reviewed the current specs in `./specs`, but the implementation is incomplete according to the plan.

A key unresolved issue is that domain events are still being triggered across multiple domains, indicating boundaries and ownership are not yet correctly enforced.
