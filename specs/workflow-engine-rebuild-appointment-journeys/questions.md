# Requirements Questions

## Q1 (2026-02-16)

For `mode=test` journeys, should appointment lifecycle events auto-trigger real test runs (strictly isolated from live runs), or should test runs only start via explicit manual start actions?
